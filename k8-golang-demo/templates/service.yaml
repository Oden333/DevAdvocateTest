apiVersion: v1
kind: Service
metadata:
  name: {{ include "k8-golang-demo.fullname" . }}
  labels:
    {{- include "k8-golang-demo.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "k8-golang-demo.selectorLabels" . | nindent 4 }}
