apiVersion: v1
kind: Secret
metadata:
  name: {{ include "k8-golang-demo.fullname" . }}-secrets
  namespace: {{ .Release.Namespace }}
  labels:
  {{- include "k8-golang-demo.labels" . | nindent 4 }}
type: Opaque
data:
  postgresPassword: {{ .Values.postgres.postgresPassword | b64enc | quote }}
